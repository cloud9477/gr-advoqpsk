id: advoqpsk_prefixer
label: prefixer
category: '[advoqpsk]'

templates:
  imports: from gnuradio import advoqpsk
  make: advoqpsk.prefixer()

inputs:
- domain: message
  id: in

outputs:
- domain: message
  id: out

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
